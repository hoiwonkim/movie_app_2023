import React from "react"; // React 모듈을 불러옴
import ReactDOM from "react-dom"; // ReactDOM 모듈을 불러옴
import App from "./App"; // App 컴포넌트를 불러옴

ReactDOM.render(<App />, document.getElementById("potato"));// App 컴포넌트를 potato id를 가진 DOM 요소에 렌더링한다.
